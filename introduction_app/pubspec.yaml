name: introduction_app
description: Introduction App, built on Nanc Backend Driven UI
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0-42.0.dev <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  nanc_renderer:
    path: ../../nanc/nanc_renderer

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
  lint:
    path: ../../nanc/lint

flutter:
  uses-material-design: true